# Formulario de contacto (opcional). Sin estas variables el formulario abre el
# correo del visitante con el mensaje prellenado (mailto).
RESEND_API_KEY=
CONTACT_TO=juanmartin@simplex.la
# Dominio verificado en Resend; si no hay, usa onboarding@resend.dev (solo manda a tu propio mail).
CONTACT_FROM=
